- root (dict):
    actions:
      actions (list, length=0):
      - actions_0 (np.array, shape=4, dtype=float64): null
      - '...'
    agent_infos:
      agent_infos (list, length=0):
      - agent_infos_0 (dict):
          hand_distance:
            hand_distance (float64): null
          hand_success:
            hand_success (float64): null
      - '...'
    env_infos:
      env_infos (list, length=0):
      - env_infos_0 (dict):
          hand_distance:
            hand_distance (float64): null
          hand_success:
            hand_success (float64): null
      - '...'
    next_observations:
      next_observations (list, length=0):
      - next_observations_0 (dict):
          achieved_goal:
            achieved_goal (np.array, shape=3, dtype=float64): null
          desired_goal:
            desired_goal (np.array, shape=3, dtype=float64): null
          hires_image_observation:
            hires_image_observation (np.array, shape=(480, 640, 3), dtype=uint8): null
          image_observation:
            image_observation (np.array, shape=(480, 640, 3), dtype=uint8): null
          observation:
            observation (np.array, shape=3, dtype=float64): null
          state_achieved_goal:
            state_achieved_goal (np.array, shape=3, dtype=float64): null
          state_desired_goal:
            state_desired_goal (np.array, shape=3, dtype=float64): null
          state_observation:
            state_observation (np.array, shape=3, dtype=float64): null
      - '...'
    observations:
      observations (list, length=0):
      - observations_0 (dict):
          achieved_goal:
            achieved_goal (np.array, shape=3, dtype=float64): null
          desired_goal:
            desired_goal (np.array, shape=3, dtype=float64): null
          hires_image_observation:
            hires_image_observation (np.array, shape=(480, 640, 3), dtype=uint8): null
          image_observation:
            image_observation (np.array, shape=(480, 640, 3), dtype=uint8): null
          observation:
            observation (np.array, shape=3, dtype=float64): null
          state_achieved_goal:
            state_achieved_goal (np.array, shape=3, dtype=float64): null
          state_desired_goal:
            state_desired_goal (np.array, shape=3, dtype=float64): null
          state_observation:
            state_observation (np.array, shape=3, dtype=float64): null
      - '...'
    rewards:
      rewards (list, length=0):
      - rewards_0 (int): null
      - '...'
    terminals:
      terminals (list, length=0):
      - terminals_0 (bool): null
      - '...'
- root (dict):
    actions:
      actions (np.array, shape=(75, 4), dtype=float32): null
    agent_infos:
      agent_infos (list, length=0):
      - agent_infos_0 (dict): {}
      - '...'
    env_infos:
      env_infos (list, length=0):
      - env_infos_0 (dict):
          ContextualEnv/old_reward:
            ContextualEnv/old_reward (int): null
          hand_distance:
            hand_distance (float64): null
          hand_success:
            hand_success (float64): null
      - '...'
    next_observations:
      next_observations (np.array, dtype=object, length=0):
      - next_observations_0 (dict):
          achieved_goal:
            achieved_goal (np.array, shape=3, dtype=float64): null
          desired_goal:
            desired_goal (np.array, shape=3, dtype=float64): null
          hires_image_observation:
            hires_image_observation (np.array, shape=(480, 640, 3), dtype=uint8): null
          image_desired_goal:
            image_desired_goal (np.array, shape=6912, dtype=float64): null
          image_observation:
            image_observation (np.array, shape=6912, dtype=float64): null
          initial_image_observation:
            initial_image_observation (np.array, shape=6912, dtype=float64): null
          initial_latent_state:
            initial_latent_state (np.array, shape=720, dtype=float32): null
          latent_desired_goal:
            latent_desired_goal (np.array, shape=720, dtype=float32): null
          latent_observation:
            latent_observation (np.array, shape=720, dtype=float32): null
          observation:
            observation (np.array, shape=3, dtype=float64): null
          presampled_latent_goals:
            presampled_latent_goals (np.array, shape=720, dtype=float32): null
          state_achieved_goal:
            state_achieved_goal (np.array, shape=3, dtype=float64): null
          state_desired_goal:
            state_desired_goal (np.array, shape=3, dtype=float64): null
          state_observation:
            state_observation (np.array, shape=3, dtype=float64): null
      - '...'
    observations:
      observations (np.array, dtype=object, length=0):
      - observations_0 (dict):
          achieved_goal:
            achieved_goal (np.array, shape=3, dtype=float64): null
          desired_goal:
            desired_goal (np.array, shape=3, dtype=float64): null
          hires_image_observation:
            hires_image_observation (np.array, shape=(480, 640, 3), dtype=uint8): null
          image_desired_goal:
            image_desired_goal (np.array, shape=6912, dtype=float64): null
          image_observation:
            image_observation (np.array, shape=6912, dtype=float64): null
          initial_image_observation:
            initial_image_observation (np.array, shape=6912, dtype=float64): null
          initial_latent_state:
            initial_latent_state (np.array, shape=720, dtype=float32): null
          latent_desired_goal:
            latent_desired_goal (np.array, shape=720, dtype=float32): null
          latent_observation:
            latent_observation (np.array, shape=720, dtype=float32): null
          observation:
            observation (np.array, shape=3, dtype=float64): null
          presampled_latent_goals:
            presampled_latent_goals (np.array, shape=720, dtype=float32): null
          state_achieved_goal:
            state_achieved_goal (np.array, shape=3, dtype=float64): null
          state_desired_goal:
            state_desired_goal (np.array, shape=3, dtype=float64): null
          state_observation:
            state_observation (np.array, shape=3, dtype=float64): null
      - '...'
    rewards:
      rewards (np.array, shape=(75, 1), dtype=int64): null
    terminals:
      terminals (np.array, shape=(75, 1), dtype=bool): null
- root (dict):
    actions:
      actions (np.array, shape=(75, 4), dtype=float32): null
    agent_infos:
      agent_infos (list, length=0):
      - agent_infos_0 (dict): {}
      - '...'
    env_infos:
      env_infos (list, length=0):
      - env_infos_0 (dict):
          ContextualEnv/old_reward:
            ContextualEnv/old_reward (int): null
          hand_distance:
            hand_distance (float64): null
          hand_success:
            hand_success (float64): null
      - '...'
    next_observations:
      next_observations (np.array, dtype=object, length=0):
      - next_observations_0 (dict):
          achieved_goal:
            achieved_goal (np.array, shape=3, dtype=float64): null
          desired_goal:
            desired_goal (np.array, shape=3, dtype=float64): null
          hires_image_observation:
            hires_image_observation (np.array, shape=(480, 640, 3), dtype=uint8): null
          image_observation:
            image_observation (np.array, shape=6912, dtype=float64): null
          initial_latent_state:
            initial_latent_state (np.array, shape=720, dtype=float32): null
          latent_desired_goal:
            latent_desired_goal (np.array, shape=720, dtype=float32): null
          latent_observation:
            latent_observation (np.array, shape=720, dtype=float32): null
          observation:
            observation (np.array, shape=3, dtype=float64): null
          presampled_latent_goals:
            presampled_latent_goals (np.array, shape=720, dtype=float32): null
          state_achieved_goal:
            state_achieved_goal (np.array, shape=3, dtype=float64): null
          state_desired_goal:
            state_desired_goal (np.array, shape=3, dtype=float64): null
          state_observation:
            state_observation (np.array, shape=3, dtype=float64): null
      - '...'
    observations:
      observations (np.array, dtype=object, length=0):
      - observations_0 (dict):
          achieved_goal:
            achieved_goal (np.array, shape=3, dtype=float64): null
          desired_goal:
            desired_goal (np.array, shape=3, dtype=float64): null
          hires_image_observation:
            hires_image_observation (np.array, shape=(480, 640, 3), dtype=uint8): null
          image_observation:
            image_observation (np.array, shape=6912, dtype=float64): null
          initial_latent_state:
            initial_latent_state (np.array, shape=720, dtype=float32): null
          latent_desired_goal:
            latent_desired_goal (np.array, shape=720, dtype=float32): null
          latent_observation:
            latent_observation (np.array, shape=720, dtype=float32): null
          observation:
            observation (np.array, shape=3, dtype=float64): null
          presampled_latent_goals:
            presampled_latent_goals (np.array, shape=720, dtype=float32): null
          state_achieved_goal:
            state_achieved_goal (np.array, shape=3, dtype=float64): null
          state_desired_goal:
            state_desired_goal (np.array, shape=3, dtype=float64): null
          state_observation:
            state_observation (np.array, shape=3, dtype=float64): null
      - '...'
    rewards:
      rewards (np.array, shape=(75, 1), dtype=int64): null
    terminals:
      terminals (np.array, shape=(75, 1), dtype=bool): null
- root (dict):
    actions:
      actions (np.array, shape=(75, 4), dtype=float32): null
    agent_infos:
      agent_infos (list, length=0):
      - agent_infos_0 (dict): {}
      - '...'
    env_infos:
      env_infos (list, length=0):
      - env_infos_0 (dict):
          ContextualEnv/old_reward:
            ContextualEnv/old_reward (int): null
          hand_distance:
            hand_distance (float64): null
          hand_success:
            hand_success (float64): null
      - '...'
    next_observations:
      next_observations (np.array, dtype=object, length=0):
      - next_observations_0 (dict):
          achieved_goal:
            achieved_goal (np.array, shape=3, dtype=float64): null
          desired_goal:
            desired_goal (np.array, shape=3, dtype=float64): null
          hires_image_observation:
            hires_image_observation (np.array, shape=(480, 640, 3), dtype=uint8): null
          image_observation:
            image_observation (np.array, shape=6912, dtype=float64): null
          initial_latent_state:
            initial_latent_state (np.array, shape=288, dtype=float32): null
          latent_desired_goal:
            latent_desired_goal (np.array, shape=288, dtype=float32): null
          latent_observation:
            latent_observation (np.array, shape=288, dtype=float32): null
          observation:
            observation (np.array, shape=3, dtype=float64): null
          state_achieved_goal:
            state_achieved_goal (np.array, shape=3, dtype=float64): null
          state_desired_goal:
            state_desired_goal (np.array, shape=3, dtype=float64): null
          state_observation:
            state_observation (np.array, shape=3, dtype=float64): null
      - '...'
    observations:
      observations (np.array, dtype=object, length=0):
      - observations_0 (dict):
          achieved_goal:
            achieved_goal (np.array, shape=3, dtype=float64): null
          desired_goal:
            desired_goal (np.array, shape=3, dtype=float64): null
          hires_image_observation:
            hires_image_observation (np.array, shape=(480, 640, 3), dtype=uint8): null
          image_observation:
            image_observation (np.array, shape=6912, dtype=float64): null
          initial_latent_state:
            initial_latent_state (np.array, shape=288, dtype=float32): null
          latent_desired_goal:
            latent_desired_goal (np.array, shape=288, dtype=float32): null
          latent_observation:
            latent_observation (np.array, shape=288, dtype=float32): null
          observation:
            observation (np.array, shape=3, dtype=float64): null
          state_achieved_goal:
            state_achieved_goal (np.array, shape=3, dtype=float64): null
          state_desired_goal:
            state_desired_goal (np.array, shape=3, dtype=float64): null
          state_observation:
            state_observation (np.array, shape=3, dtype=float64): null
      - '...'
    rewards:
      rewards (np.array, shape=(75, 1), dtype=int64): null
    terminals:
      terminals (np.array, shape=(75, 1), dtype=bool): null
- root (dict):
    actions:
      actions (np.array, shape=(200, 4), dtype=float32): null
    agent_infos:
      agent_infos (list, length=0):
      - agent_infos_0 (dict): {}
      - '...'
    env_infos:
      env_infos (list, length=0):
      - env_infos_0 (dict):
          ContextualEnv/old_reward:
            ContextualEnv/old_reward (int): null
          hand_distance:
            hand_distance (float64): null
          hand_success:
            hand_success (float64): null
      - '...'
    next_observations:
      next_observations (np.array, dtype=object, length=0):
      - next_observations_0 (dict):
          achieved_goal:
            achieved_goal (np.array, shape=3, dtype=float64): null
          desired_goal:
            desired_goal (np.array, shape=3, dtype=float64): null
          hires_image_observation:
            hires_image_observation (np.array, shape=(480, 640, 3), dtype=uint8): null
          image_desired_goal:
            image_desired_goal (np.array, shape=6912, dtype=float64): null
          image_observation:
            image_observation (np.array, shape=6912, dtype=float64): null
          initial_image_observation:
            initial_image_observation (np.array, shape=6912, dtype=float64): null
          initial_latent_state:
            initial_latent_state (np.array, shape=720, dtype=float32): null
          latent_desired_goal:
            latent_desired_goal (np.array, shape=720, dtype=float32): null
          latent_observation:
            latent_observation (np.array, shape=720, dtype=float32): null
          observation:
            observation (np.array, shape=3, dtype=float64): null
          state_achieved_goal:
            state_achieved_goal (np.array, shape=3, dtype=float64): null
          state_desired_goal:
            state_desired_goal (np.array, shape=3, dtype=float64): null
          state_observation:
            state_observation (np.array, shape=3, dtype=float64): null
      - '...'
    observations:
      observations (np.array, dtype=object, length=0):
      - observations_0 (dict):
          achieved_goal:
            achieved_goal (np.array, shape=3, dtype=float64): null
          desired_goal:
            desired_goal (np.array, shape=3, dtype=float64): null
          hires_image_observation:
            hires_image_observation (np.array, shape=(480, 640, 3), dtype=uint8): null
          image_desired_goal:
            image_desired_goal (np.array, shape=6912, dtype=float64): null
          image_observation:
            image_observation (np.array, shape=6912, dtype=float64): null
          initial_image_observation:
            initial_image_observation (np.array, shape=6912, dtype=float64): null
          initial_latent_state:
            initial_latent_state (np.array, shape=720, dtype=float32): null
          latent_desired_goal:
            latent_desired_goal (np.array, shape=720, dtype=float32): null
          latent_observation:
            latent_observation (np.array, shape=720, dtype=float32): null
          observation:
            observation (np.array, shape=3, dtype=float64): null
          state_achieved_goal:
            state_achieved_goal (np.array, shape=3, dtype=float64): null
          state_desired_goal:
            state_desired_goal (np.array, shape=3, dtype=float64): null
          state_observation:
            state_observation (np.array, shape=3, dtype=float64): null
      - '...'
    rewards:
      rewards (np.array, shape=(200, 1), dtype=int64): null
    terminals:
      terminals (np.array, shape=(200, 1), dtype=bool): null
- root (dict):
    actions:
      actions (np.array, shape=(75, 4), dtype=float32): null
    agent_infos:
      agent_infos (list, length=0):
      - agent_infos_0 (dict): {}
      - '...'
    env_infos:
      env_infos (list, length=0):
      - env_infos_0 (dict):
          ContextualEnv/old_reward:
            ContextualEnv/old_reward (int): null
          hand_distance:
            hand_distance (float64): null
          hand_success:
            hand_success (float64): null
      - '...'
    next_observations:
      next_observations (np.array, dtype=object, length=0):
      - next_observations_0 (dict):
          achieved_goal:
            achieved_goal (np.array, shape=3, dtype=float64): null
          desired_goal:
            desired_goal (np.array, shape=3, dtype=float64): null
          hires_image_observation:
            hires_image_observation (np.array, shape=(480, 640, 3), dtype=uint8): null
          image_desired_goal:
            image_desired_goal (np.array, shape=6912, dtype=float64): null
          image_observation:
            image_observation (np.array, shape=6912, dtype=float64): null
          initial_image_observation:
            initial_image_observation (np.array, shape=6912, dtype=float64): null
          initial_latent_state:
            initial_latent_state (np.array, shape=288, dtype=float32): null
          latent_desired_goal:
            latent_desired_goal (np.array, shape=288, dtype=float32): null
          latent_observation:
            latent_observation (np.array, shape=288, dtype=float32): null
          observation:
            observation (np.array, shape=3, dtype=float64): null
          state_achieved_goal:
            state_achieved_goal (np.array, shape=3, dtype=float64): null
          state_desired_goal:
            state_desired_goal (np.array, shape=3, dtype=float64): null
          state_observation:
            state_observation (np.array, shape=3, dtype=float64): null
      - '...'
    observations:
      observations (np.array, dtype=object, length=0):
      - observations_0 (dict):
          achieved_goal:
            achieved_goal (np.array, shape=3, dtype=float64): null
          desired_goal:
            desired_goal (np.array, shape=3, dtype=float64): null
          hires_image_observation:
            hires_image_observation (np.array, shape=(480, 640, 3), dtype=uint8): null
          image_desired_goal:
            image_desired_goal (np.array, shape=6912, dtype=float64): null
          image_observation:
            image_observation (np.array, shape=6912, dtype=float64): null
          initial_image_observation:
            initial_image_observation (np.array, shape=6912, dtype=float64): null
          initial_latent_state:
            initial_latent_state (np.array, shape=288, dtype=float32): null
          latent_desired_goal:
            latent_desired_goal (np.array, shape=288, dtype=float32): null
          latent_observation:
            latent_observation (np.array, shape=288, dtype=float32): null
          observation:
            observation (np.array, shape=3, dtype=float64): null
          state_achieved_goal:
            state_achieved_goal (np.array, shape=3, dtype=float64): null
          state_desired_goal:
            state_desired_goal (np.array, shape=3, dtype=float64): null
          state_observation:
            state_observation (np.array, shape=3, dtype=float64): null
      - '...'
    rewards:
      rewards (np.array, shape=(75, 1), dtype=int64): null
    terminals:
      terminals (np.array, shape=(75, 1), dtype=bool): null
- root (dict):
    actions:
      actions (list, length=0):
      - actions_0 (np.array, shape=4, dtype=float64): null
      - '...'
    agent_infos:
      agent_infos (list, length=0):
      - agent_infos_0 (dict):
          hand_distance:
            hand_distance (float64): null
          hand_success:
            hand_success (float64): null
      - '...'
    env_infos:
      env_infos (list, length=0):
      - env_infos_0 (dict):
          hand_distance:
            hand_distance (float64): null
          hand_success:
            hand_success (float64): null
      - '...'
    next_observations:
      next_observations (list, length=0):
      - next_observations_0 (dict):
          achieved_goal:
            achieved_goal (np.array, shape=3, dtype=float64): null
          desired_goal:
            desired_goal (np.array, shape=3, dtype=float64): null
          image_observation:
            image_observation (np.array, shape=(480, 640, 3), dtype=uint8): null
          observation:
            observation (np.array, shape=3, dtype=float64): null
          state_achieved_goal:
            state_achieved_goal (np.array, shape=3, dtype=float64): null
          state_desired_goal:
            state_desired_goal (np.array, shape=3, dtype=float64): null
          state_observation:
            state_observation (np.array, shape=3, dtype=float64): null
      - '...'
    observations:
      observations (list, length=0):
      - observations_0 (dict):
          achieved_goal:
            achieved_goal (np.array, shape=3, dtype=float64): null
          desired_goal:
            desired_goal (np.array, shape=3, dtype=float64): null
          image_observation:
            image_observation (np.array, shape=(480, 640, 3), dtype=uint8): null
          observation:
            observation (np.array, shape=3, dtype=float64): null
          state_achieved_goal:
            state_achieved_goal (np.array, shape=3, dtype=float64): null
          state_desired_goal:
            state_desired_goal (np.array, shape=3, dtype=float64): null
          state_observation:
            state_observation (np.array, shape=3, dtype=float64): null
      - '...'
    rewards:
      rewards (list, length=0):
      - rewards_0 (int): null
      - '...'
    terminals:
      terminals (list, length=0):
      - terminals_0 (bool): null
      - '...'
